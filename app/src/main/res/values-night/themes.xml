<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CuidadoCompartidoMayor" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/main_color_dark</item>
        <item name="colorOnPrimary">#000000</item>
        <item name="colorPrimaryContainer">#004881</item>

        <!-- Colores de fondo y superficie -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>

        <!-- Acento (usando tu accent_claro o uno específico para dark) -->
        <item name="colorAccent">@color/accent_claro</item>

        <!-- Personalización de Status Bar y Navigation Bar -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>


    </style>
</resources>