<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/medical_appointment_stroke">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Icono de cita médica -->
        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_medical"
            android:background="@drawable/circle_medical_appointment"
            android:padding="8dp"
            app:tint="@android:color/white"
            android:contentDescription="Cita médica"/>

        <!-- Información de la cita -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <TextView
                android:id="@+id/tvAppointmentTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/medical_appointment_stroke"
                tools:text="10:30 AM"/>

            <TextView
                android:id="@+id/tvAppointmentTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="?android:attr/textColorPrimary"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="4dp"
                tools:text="Consulta con Dr. García"/>

            <TextView
                android:id="@+id/tvAppointmentLocation"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="?android:attr/textColorSecondary"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="2dp"
                android:drawableStart="@drawable/ic_location_pin"
                android:drawablePadding="4dp"
                android:drawableTint="?android:attr/textColorSecondary"
                tools:text="Hospital General"/>

        </LinearLayout>

        <!-- Flecha para indicar que es clickeable -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_chevron_right"
            app:tint="?android:attr/textColorSecondary"
            android:contentDescription="Ver detalles"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
