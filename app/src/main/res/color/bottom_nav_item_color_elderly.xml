<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color cuando está seleccionado -->
    <item android:color="@color/green_one" android:state_checked="true" />
    <!-- Color cuando está presionado -->
    <item android:color="@color/blue_darker" android:state_pressed="true" />
    <!-- Color normal -->
    <item android:color="@color/gray_tiny" />
</selector>