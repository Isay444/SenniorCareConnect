<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisos para el sistema de emergencia -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permiso para el qr -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Permisos para notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permisos para alarmas exactas -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ExactAlarmPolicy" />

    <!-- ✅ Permiso para recibir BOOT_COMPLETED (reiniciar alarmas después de reiniciar dispositivo) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ✅ Permiso para mantener la app despierta (opcional) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permisos para almacenamiento (Android 12 y anteriores) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_care_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_care_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CuidadoCompartidoMayor"
        tools:targetApi="31"

        android:name=".MedicationApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.caregiver.CaregiverDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.CuidadoCompartidoMayor" />

        <!-- Dashboard del Adulto Mayor -->
        <activity
            android:name=".ui.elderly.ElderlyDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ElderlyDashboard"
            android:windowSoftInputMode="adjustPan" />

        <!-- BroadcastReceiver para alarmas -->
        <receiver
            android:name=".notifications.MedicationAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="MEDICATION_ALARM" />
                <!-- Para reprogramar después de reiniciar -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver para reiniciar el generador de logs -->
        <receiver
            android:name=".utils.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider para compartir PDFs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>